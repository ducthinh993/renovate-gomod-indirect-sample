{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:base"
  ],
  "packageRules": [
    {
      "matchPackagePatterns": ["*"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "all non-major dependencies",
      "groupSlug": "all-minor-patch"
    },
    {
      "matchPackagePatterns": ["*"],
      "matchUpdateTypes": ["major"],
      "groupName": "all major dependencies",
      "groupSlug": "all-major"
    }
  ],
  "postUpdateOptions": [
    "goModTidyAll"
  ],
  "timezone": "UTC",
  "prHourlyLimit": 2,
  "prConcurrentLimit": 10,
  "rangeStrategy": "auto",
  "semanticCommits": "enabled",
  "commitMessagePrefix": "chore(deps):",
  "commitMessageAction": "update",
  "commitMessageTopic": "dependencies",
  "commitMessageExtra": "to {{#if isSingleHash}}{{newVersion}}{{else}}{{#each upgrades as |upgrade|}}{{#if @first}}{{upgrade.newVersion}}{{else}}, {{upgrade.newVersion}}{{/if}}{{/each}}{{/if}}",
  "commitMessageSuffix": "",
  "prTitle": "{{#if isPin}}Pin {{/if}}{{#if isLockfileUpdate}}Lock file maintenance{{else if isRange}}Update {{/if}}{{depName}}{{#if newValue}} to {{newValue}}{{/if}}{{#if isLockfileUpdate}}{{else if isRange}} to {{newVersion}}{{/if}}",
  "prBodyTemplate": "{{#if isLockfileUpdate}}## Lock file maintenance\n\nThis PR contains lock file maintenance updates to ensure all dependencies are up to date.{{else}}## Changes\n\nThis PR updates {{depName}} from {{currentVersion}} to {{newVersion}}.\n\n{{#if hasReleaseNotes}}## Release Notes\n\n{{releaseNotes}}{{/if}}\n\n{{#if hasReleaseNotes}}{{else}}### What's Changed\n\n{{#each upgrades as |upgrade|}}- **{{upgrade.depName}}**: {{upgrade.currentVersion}} → {{upgrade.newVersion}}{{#if upgrade.hasReleaseNotes}} ([release notes]({{upgrade.releaseNotes}})){{/if}}\n{{/each}}{{/if}}\n\n{{#if hasReleaseNotes}}{{else}}### Additional Notes\n\nThis update was automatically generated by Renovate.{{/if}}{{/if}}\n\n{{#if isPin}}**Note**: This is an automated dependency pin update.{{/if}}",
  "labels": [
    "dependencies",
    "go"
  ],
  "assignees": [],
  "reviewers": [],
  "automerge": false,
  "automergeType": "pr",
  "platformAutomerge": false,
  "gitIgnoredAuthors": [],
  "onboarding": true,
  "requireConfig": false,
  "repositories": [
    "."
  ]
}